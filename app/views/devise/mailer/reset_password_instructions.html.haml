%p= t('devise.mailer.hello')
= @resource.email

%p= t('devise.mailer.link_was_requested')

%p= link_to t('devise.mailer.change'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)

%p= t('devise.mailer.ignore_if_didnt_request')
%p= t('devise.mailer.will_not_change_until')
